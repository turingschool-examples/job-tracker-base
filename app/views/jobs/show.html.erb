Title: <%= @job.title %>
<br>

Description: <%= @job.description %>
<br>

Level of Interest: <%= @job.level_of_interest %>
<br>

City: <%= @job.city %>
<br>

Company: <%= link_to @job.company.name, company_path(@job.company) %>

Tags:
<% @tags.each do |tag| %>
<table>
	<thead>
		<tr>
			<th>Name</th>
			<th>Number of Jobs with this Tag</th>
			<th>Average salary of Jobs with this Tag</th>
		</tr>
	</thead>
	<tbody>
		<tr class="tags-table-<%= tag.id %>">
			<td><%= tag.name %></td>
			<td><%= tag.jobs.count %></td>
			<td>$<%= tag.avg_salary %></td>
		</tr>
	</tbody>
</table>
<% end %>